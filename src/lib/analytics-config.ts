/**
 * Public GA4 measurement ID (safe for client bundles — same class of secret as
 * HubSpot portal ID). Used only on canonical production hosts unless
 * NEXT_PUBLIC_GA_TRACKING=force is set for local verification.
 */
export const GA_MEASUREMENT_ID = "G-G61027WSXF";
